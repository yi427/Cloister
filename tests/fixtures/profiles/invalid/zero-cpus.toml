schema_version = 5
name = "zero-cpus"
image = { reference = "cloister:dev", architecture = "arm64" }
guest = { cpus = 0, memory = "8G", user = "cloister", locale = "en_US.UTF-8", timezone = "America/New_York" }
network = { mode = "default" }
agent = { state = "isolated" }
host = { exec = { enabled = true, environment = { mode = "inherit-all" }, allow = [] } }
