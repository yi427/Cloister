schema_version = 2
name = "invalid-memory"
image = { reference = "cloister/rust-node:dev", architecture = "arm64" }
guest = { hostname = "cloister", cpus = 4, memory = "hello", user = "cloister", locale = "zh_CN.UTF-8", timezone = "Asia/Shanghai" }
network = { mode = "default" }
agents = { codex = { enabled = true, state = "isolated" }, claude = { enabled = true, state = "isolated" } }
