schema_version = 3
name = "invalid-memory"
image = { reference = "cloister/rust-node:dev", architecture = "arm64" }
guest = { cpus = 4, memory = "hello", user = "cloister", locale = "zh_CN.UTF-8", timezone = "Asia/Shanghai" }
network = { mode = "default" }
codex = { state = "isolated" }
